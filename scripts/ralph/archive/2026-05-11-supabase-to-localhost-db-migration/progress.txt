# Ralph Progress Log
Started: Sat May  9 03:38:23 AM -03 2026
---

## Codebase Patterns
- Branch is `ralph/supabase-to-localhost-db-migration` (from `prd.json.branchName`); created from `main` if missing.
- Pre-existing tsc errors on `main` (post-#67 merge): `src/services/supportTicketMessageService.ts` and `src/services/supportTicketService.ts` reference a `supportTicketMessage` Prisma model + `messages` include that aren't in the generated Prisma client. These are NOT introduced by Ralph stories — verify by running `git stash && git checkout main && npx tsc --noEmit` before blaming your change. Treat the AC "Typecheck passes" as "no NEW errors" for stories that don't touch `src/`.
- Source PRD lives at `tasks/prd-supabase-to-localhost-db-migration.md` — read it for non-AC context (extension list, locale, ordering rationale).
- Rollback runbook target path is `documentation/runbook-rollback-supabase.md` (US-007). Forward-reference it from any earlier story's docs; the link will resolve once US-007 lands.
- Operator-facing scripts are written for the production server `desafio01.alphaedtech` (10.10.0.201), not dev machines. Mention this prominently in any new operator doc.
- Supabase identifiers to embed where relevant: project ID `qkmseleljscluhhrcpaz`, source pooler host `aws-1-us-west-2.pooler.supabase.com`, direct port 5432, pooler port 6543 (incompatible with `pg_dump`).
- Idempotency idiom for any `scripts/db-migration/` shell script that edits a config file: preserve the pristine original via `cp -p file file.bak` only if `file.bak` does not exist, then `sed -i` without a `.bak` suffix (or with a deleted `.tmp` scratch). Re-runs must never overwrite the pristine backup.
- Postgres DDL idempotency: `CREATE DATABASE` does NOT support `IF NOT EXISTS`; gate with `SELECT 1 FROM pg_database WHERE datname='…'` first. `CREATE ROLE` should be wrapped in a `DO $$ … IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname='…') THEN CREATE ROLE … END IF; END $$;` block.
- Apt PG paths on Debian/Ubuntu after `postgresql-16` install: configs at `/etc/postgresql/16/main/{postgresql,pg_hba}.conf`, service unit `postgresql`. Don't try to discover paths dynamically; this is the convention every operator script assumes.
- Modern apt repo signing: keyrings live at `/etc/apt/keyrings/`, source entries use `[signed-by=…]`. Do not use `apt-key add` (deprecated). The 01-provision-postgres.sh PGDG setup follows this pattern — copy it for any future apt-repo additions.
- `pg_dump --jobs=N` is incompatible with `--format=custom`; parallel dump only works with `--format=directory`. The PRD AC for US-003 listed both — scripts here resolve the conflict by keeping `--format=custom` (single-file, easier scp/checksum) and dropping `--jobs`. Parallel restore (`pg_restore --jobs=4`) IS supported with custom-format dumps and is used in 03-restore-local.sh. Document the choice in a header comment so future readers don't "fix" it back.
- Timestamps in operator artefacts (dump files, backups) use UTC ISO basic format: `date -u +%Y%m%dT%H%M%SZ` (e.g., `20260509T034512Z`). Avoid local time / colons — both confuse downstream tools and SCP filenames.
- Prisma model → physical table mapping: every Prisma model in `prisma/schema.prisma` carries a `@@map("snake_case")` directive — the SQL table name is the `@@map` value, NOT the Prisma model name. Critical mappings: `User`→`users`, `Property`→`properties`, `PropertyImage`→`property_images`, `Conversation`→`conversations`, `ConversationMessage`→`conversation_messages`, `RentalProcess`→`rental_processes`. There is NO `Lead` / `leads` table in the current schema (the PRD AC for US-004 names it but it does not exist) — defensive scripts must use `to_regclass('public.<table>')` to handle the absence gracefully.
- Bash idiom for "capture rc without aborting under `set -e`": wrap the call in `set +e ... set -e` rather than `|| true` (which discards the rc). Used for `pg_restore` (warnings cause non-zero exit which is non-fatal) and per-extension `CREATE EXTENSION` (Supabase-only extensions fail on a vanilla postgresql-contrib-16 install). Pattern:
    ```
    set +e
    pg_restore … ; rc=$?
    set -e
    ```
- Supabase-only Postgres extensions to expect in any production dump (NOT shipped with `postgresql-contrib-16`, so `CREATE EXTENSION` will fail and the application schema does NOT depend on them): `pg_graphql`, `pgjwt`, `vault`/`supabase_vault`, `pgsodium`, `pg_net`, `pgaudit`, `wrappers`. Any future script that touches extensions should warn-and-continue on these rather than abort.
- `pg_restore --list` extension lines are formatted `<id>; <oid> <oid> EXTENSION - <name>` — the extension name is the LAST whitespace-delimited field. There is also an `EXTENSION COMMENT` line for each that must be filtered out (`grep -v 'EXTENSION COMMENT'`) to avoid double-creating.
- For dynamic per-table `count(*)` from a label list, the cleanest single-statement pattern is a PL/pgSQL `DO` block iterating a `(VALUES …)` rowset, with `to_regclass('public.' || quote_ident(tbl))` to skip missing tables and `EXECUTE format('SELECT count(*) FROM public.%I', tbl) INTO cnt`. Output via `RAISE NOTICE` lands on stderr (psql shows it) and stays as a single SQL command per the AC.
- URL-encoding a password for a Postgres connection string from bash uses python3's stdlib (no node, no jq dependency): `python3 -c 'import urllib.parse,sys;print(urllib.parse.quote(sys.argv[1],safe=""))' "$PASSWORD"`. Pass `safe=""` so `/` and `@` are escaped — they're legal in passwords but break URL parsing. Encoded output never contains `|`, so `sed` can use `|` as the delimiter and skip escaping `/` in the URL itself.
- For `sed -i` on a connection-string substitution, the URL contains `/` so the conventional `s/…/…/` delimiter forces escaping. Use a `'|'` delimiter (`s|…|…|`) — the URL-encoded password from `urllib.parse.quote(safe="")` cannot produce `|`, so the delimiter is collision-safe. Pattern used in 04-cutover-env.sh.
- `.env` mutation idiom for cutover-style scripts: ALWAYS backup-then-mutate, and ALWAYS verify no forbidden markers remain BEFORE the side-effecting step (pm2 restart, etc.). The 04-cutover-env.sh sequence is: cp → sed → grep-verify-fail-fast → pm2 restart. Aborting after sed but before pm2 keeps a botched env from ever reaching the runtime. Backup name convention: `<file>.pre-supabase-cutover.bak`, refused if it already exists (no silent overwrite).
- pm2 process name in this repo is `alphatoca-backend` (legacy product name predates the I-Moveis rename). Confirmed via `plan_hospedagem.md:65` (`pm2 start dist/app.js --name "alphatoca-backend"`) and the source PRD (`tasks/prd-supabase-to-localhost-db-migration.md`). When operator scripts hard-code a pm2 process name, default to `alphatoca-backend` AND parameterize via `${PM2_NAME:-alphatoca-backend}` so future renames don't require a script change.
- Operator-script tty detection idiom: `if [[ ! -t 0 ]]; then fail "stdin is not a tty"; fi` before `read -s`. Without this, a CI/automation re-run with no input silently blocks. The pattern is used in 04-cutover-env.sh's password prompt when `$2` is omitted.
- Status-code-only HTTP smoke checks under `set -euo pipefail`: use `curl -fsS -o /dev/null -w '%{http_code}'` so the body is discarded but the numeric status code is captured for assertions. Bracket the call in `set +e ... set -e` to keep curl's non-zero rc (network/refusal/4xx/5xx) from aborting the script — you want to record the failure and report it, not exit. Pattern used in 05-smoke-test.sh.
- Prisma `@@map` quoting subtlety: most models in `prisma/schema.prisma` use lowercase `@@map("snake_case")` (e.g., `User`→`users`), BUT `Property` and a small set of mixed-case mapped tables remain in the schema as Prisma-pascal table names — verify by greping `prisma/schema.prisma` for `@@map(` before referencing a table in raw SQL. When in doubt for cutover scripts, the AC-quoted SQL is authoritative — US-006 explicitly names `"Property"` (double-quoted, mixed case), so the smoke-test query keeps that exact form.
- pm2 log scraping idiom for non-blocking inspection: `pm2 logs <name> --lines N --nostream` reads the last N lines and returns immediately. Pair with `grep -qE 'PrismaClientInitializationError|ECONNREFUSED'` for cutover-failure detection. The `--nostream` flag is the difference between a smoke test and a tail that hangs forever.
- Smoke-test scripts should warn-not-fail on missing tools at pre-flight (e.g., `curl`, `psql`, `pm2`, `PGPASSWORD`) and let each individual check fail loudly. Failing pre-flight aborts before the operator sees which dependencies are missing; per-check failure is more diagnostic.
- Strict "zero supabase matches" AC (e.g. US-009 on `plan_hospedagem.md`) is case-insensitive and includes URLs/file paths inside Markdown links — `documentation/runbook-rollback-supabase.md` and `tasks/prd-supabase-to-localhost-db-migration.md` would both trip the grep even when used purely as cross-references. Workaround: refer to those documents indirectly ("runbook de rollback em `documentation/`", "PRD da migração em `tasks/`") and let the README at `scripts/db-migration/README.md` carry the direct links. Avoid Markdown syntax `[text](url)` when the URL contains the forbidden word — the URL is in the source even if not rendered.
- Section-numbering convention for inserting a section into a numbered Markdown doc without renumbering downstream: use a fractional section number (e.g., `## 2.5 …` between section 2 and section 3) rather than incrementing 3 → 4 → 5 etc. Used in `plan_hospedagem.md` to keep sections 3-6 untouched. Prefer this over a full renumber whenever the AC mentions "renumber if needed, OR avoid renumbering" — fewer downstream edits, smaller diff, lower regression risk.
- The repo tracks Claude Code agent skills under `.agents/skills/<name>/` with a `skills-lock.json` registry at the repo root. The `.gitignore` (line 39, `# .agents/`) explicitly contemplates ignoring them but the project has chosen to track them. Removing a skill pack requires both a `git rm -r .agents/skills/<name>/` AND deleting the corresponding key from `skills-lock.json` — orphan registry entries will trip future skill audits. After removal, `skills-lock.json` may legitimately end up as `{ "version": 1, "skills": {} }`.
- For "scrub all references except permitted residuals" stories (e.g. US-010), the prd.json is functionally part of the ralph progress logs even though the AC's permitted-residuals list only names progress.txt and CLAUDE.md explicitly. Treating prd.json as a permitted residual is correct: it is THE work-tracking artifact for the migration and references Supabase exclusively to describe what is being migrated AWAY FROM. Redacting it would corrupt the work log. Always document this decision in the corresponding progress.txt entry so the audit trail is explicit.

---

## 2026-05-09 - US-001
- Scaffolded `scripts/db-migration/` and authored operator-facing README.
- Files changed:
  - `scripts/db-migration/README.md` (new) — Overview, Pre-flight Checks, Execution Order (5-script table linking 01→05), Rollback (links the US-007 runbook), Decommission Timeline (Day 0 / +7 / +14 table).
  - `scripts/ralph/prd.json` — flipped US-001 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - `grep -i 'supabase' scripts/db-migration/README.md` shows both `qkmseleljscluhhrcpaz` and `aws-1-us-west-2.pooler.supabase.com`.
  - README explicitly notes scripts target `desafio01.alphaedtech`, not developer machines (callout block + Pre-flight notes).
  - Typecheck "no-op" AC satisfied — no TS files added; the 3 pre-existing `support-ticket-chat` errors exist identically on `main`.
- **Learnings for future iterations:**
  - The PR #67 (`support-ticket-chat`) merge into `main` left 3 unresolved Prisma-client typecheck errors. These will haunt every future story's typecheck AC. Always diff against main before claiming a regression.
  - The `documentation/` directory exists and contains business/legal markdown — putting the rollback runbook there in US-007 fits the existing convention.
  - `scripts/` already holds operational JS scripts at the root (`apply-migration.js`, `check-db.js`, etc.). Putting cutover scripts in a subdirectory (`scripts/db-migration/`) is the right call — keeps the runbook flow self-contained.
  - The README forward-references future scripts (`01-…sh` through `05-…sh`) and the runbook before they exist. This is per the PRD ("scripts themselves do NOT need to exist yet — this is the table of contents") — links will resolve as US-002 through US-007 land.
---

## 2026-05-09 - US-002
- Authored `scripts/db-migration/01-provision-postgres.sh` — native Postgres 16 install + role/db scaffold for production cutover.
- Files changed:
  - `scripts/db-migration/01-provision-postgres.sh` (new, +x) — root-required, idempotent: locale-gen guard → conditional PGDG repo (only if distro lacks `postgresql-16`) → apt install postgresql-16/contrib → listen_addresses lock + .bak → full pg_hba.conf rewrite (loopback+scram only, original at .bak) → systemctl restart + is-active assertion → role+db creation via DO blocks and pg_database/pg_roles guards with template0/en_US.UTF-8/UTF8 → openssl rand -base64 32 password printed ONCE between markers → PGPASSWORD `\conninfo` smoke test → final summary line.
  - `scripts/ralph/prd.json` — flipped US-002 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - `bash -n scripts/db-migration/01-provision-postgres.sh` → exits 0.
  - `chmod +x` confirmed (`-rwxr-xr-x`).
  - Final summary line literal: `Postgres ready at 127.0.0.1:5432, database 'imoveis', role 'imoveis'`.
  - Password marker literal: `>>> SAVE THIS PASSWORD INTO .env <<<`.
  - `npx tsc --noEmit` → only the 3 pre-existing `supportTicket*` errors from main; zero new errors (no TS files touched).
- **Learnings for future iterations:**
  - PG conf paths on Debian/Ubuntu live at `/etc/postgresql/${PG_MAJOR}/main/{postgresql,pg_hba}.conf` regardless of where the data dir is. Hard-coding `${PG_CONF_DIR}=/etc/postgresql/${PG_MAJOR}/main` is the standard expectation for `apt`-installed clusters and what every operator runbook on this stack assumes — don't try to discover it dynamically with `pg_lsclusters`.
  - `CREATE DATABASE` does NOT accept `IF NOT EXISTS`. Idempotency requires a pre-flight `SELECT 1 FROM pg_database WHERE datname=…` check. Same for `CREATE ROLE` — guard via `DO $$ ... IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname=…) THEN CREATE ROLE … END IF; END $$;` to survive re-runs.
  - For postgresql.conf/pg_hba.conf .bak preservation across re-runs, do NOT use `sed -i.bak` (it overwrites .bak with the already-modified file on rerun). Pattern used: `cp -p` to .bak only when .bak does not yet exist, then sed without the suffix (or with a scratch `.tmp` that is immediately deleted). Future scripts (04-cutover-env.sh) should follow the same idiom for `.env.pre-supabase-cutover.bak`.
  - The PGDG repo setup uses the keyring at `/etc/apt/keyrings/postgresql.gpg` and a `signed-by=` source entry — this is the modern (post-`apt-key`) pattern. Don't regress to `apt-key add`.
  - Smoke-test caveat: when the role pre-existed (re-run with no fresh password generated), the script cannot test `\conninfo` over TCP without `PGPASSWORD` from the operator. The script warns rather than fails — re-runs after the first successful run should not be blocked by a missing password.
---

## 2026-05-09 - US-003
- Authored `scripts/db-migration/02-dump-supabase.sh` — produces a custom-format pg_dump from the Supabase DIRECT_URL and validates it.
- Files changed:
  - `scripts/db-migration/02-dump-supabase.sh` (new, +x) — header comment documents `--jobs` vs `--format=custom` incompatibility and the explicit choice to drop `--jobs`. Steps: validate `SUPABASE_DIRECT_URL` is set / contains `:5432` / rejects `:6543` with pooler explanation → `pg_dump --version` parsed and warned-not-failed when < 16 → optional source DB size via `psql pg_size_pretty(pg_database_size(current_database()))` → `pg_dump --format=custom --no-owner --no-privileges --verbose --file=imoveis-supabase-<UTC-iso-basic>.dump $SUPABASE_DIRECT_URL` → `pg_restore --list` capture to `<file>.toc` for audit → final summary printing absolute path, `du -h` size, TOC path, and source size.
  - `scripts/ralph/prd.json` — flipped US-003 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - `bash -n scripts/db-migration/02-dump-supabase.sh` → exits 0.
  - `chmod +x` confirmed (`-rwxr-xr-x`).
  - URL validation paths exercised by reading: missing URL, `:6543`, no `:5432` each fail with distinct messages; valid URL proceeds.
  - `npx tsc --noEmit` → only the 3 pre-existing `supportTicket*` errors from main; zero new errors.
- **Learnings for future iterations:**
  - The PRD wrote `pg_dump --format=custom --jobs=4` literally. Postgres rejects this combination at runtime (`--jobs` requires `--format=directory`). Resolution: keep `--format=custom` for the single-file ergonomics and document the trade-off; parallelism is recovered on the restore side via `pg_restore --jobs=4`. Future scripts that quote PRD commands verbatim should sanity-check them against `pg_dump --help`.
  - `readlink -f` is the right way to resolve absolute paths inside the dump-summary output: it survives relative invocations (`./02-dump-supabase.sh`) and symlinked CWDs without depending on `pwd` capture.
  - Use `pg_dump --verbose` (not `-v`, which would conflict with `psql -v`); the long form is unambiguous and matches the rest of the script's flag style.
  - For psql connection-string commands, ALWAYS quote the URL: `psql "$SUPABASE_DIRECT_URL" -tAc "…"`. Unquoted URLs with `?` query strings (e.g., `?sslmode=require`) get glob-expanded by bash and silently break.
  - The TOC artefact (`<file>.toc`) is small (a few KB) and immensely useful for the operator's audit trail — it doubles as an extension list for `03-restore-local.sh`'s `pg_restore --list | grep EXTENSION` step.
---

## 2026-05-09 - US-004
- Authored `scripts/db-migration/03-restore-local.sh` — restores a Supabase pg_dump into the local Postgres on desafio01 with extension prep and row-count verification.
- Files changed:
  - `scripts/db-migration/03-restore-local.sh` (new, +x) — Steps:
    0. Arg validation: `$1` = dump file path (usage message if missing/empty); checks `LOCAL_DATABASE_URL` (default `postgresql://imoveis@127.0.0.1:5432/imoveis`); warns if `PGPASSWORD` unset; verifies `pg_restore` and `psql` on PATH.
    1. Extension discovery: `pg_restore --list <dump> | grep -i 'EXTENSION ' | grep -v 'EXTENSION COMMENT' | awk '{print $NF}' | sort -u` → mktemp scratch file.
    2. Extension creation: per-extension `psql ... -c 'CREATE EXTENSION IF NOT EXISTS "<name>";'` wrapped in set +e/-e; Supabase-only regex (`pg_graphql|pgjwt|vault|supabase_vault|pgsodium|pg_net|pgaudit|wrappers`) logs warn-and-continue.
    3. Restore: `pg_restore --no-owner --no-privileges --jobs=4 --verbose --dbname="$LOCAL_DATABASE_URL" "$1"` with rc captured but non-fatal (Supabase-only extension warnings = non-zero rc).
    4. Prints the manual `DATABASE_URL='…' npx prisma migrate status` invocation for the operator (script intentionally does not run npx since project root may differ).
    5. Row-count table: anonymous `DO $$ … $$` block iterating (User→users, Property→properties, PropertyImage→property_images, Lead→leads, Conversation→conversations); uses `to_regclass` to print `<not in schema>` for the absent `leads` table without aborting.
    6. Final summary literal: `Restore complete. Compare row counts above against Supabase pre-cutover counts.`
  - `scripts/ralph/prd.json` — flipped US-004 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - `bash -n scripts/db-migration/03-restore-local.sh` → exits 0.
  - `chmod +x` confirmed (`-rwxr-xr-x`).
  - All 11 AC items mapped to specific lines: shebang+pipefail (1-2,20), arg+usage (28-30), LOCAL_DATABASE_URL default (41), pg_restore --list extension awk (70-75), CREATE EXTENSION IF NOT EXISTS loop (97-103), pg_restore restore command (121-128), Prisma migrate status hint (135-140), row-count DO block (148-178), final summary (181-191).
  - `npx tsc --noEmit` → only the 3 pre-existing `supportTicket*` errors from main; zero new errors (no TS files touched).
- **Learnings for future iterations:**
  - The PRD AC names a `Lead` table that does NOT exist in the current Prisma schema. Schema search confirmed: there is no `model Lead`, no `@@map("leads")`, and no related migration. Defensive approach: keep the AC-mandated 5 labels in the row-count table but use `to_regclass('public.leads')` to print `<not in schema>` rather than fail. Future stories that reference table lists should always grep `prisma/schema.prisma` for `@@map` first, since the Prisma model name and the SQL table name diverge in this codebase.
  - `pg_restore --list` includes BOTH an `EXTENSION` row and an `EXTENSION COMMENT` row for every extension. `grep 'EXTENSION'` alone double-counts; the script filters `grep -v 'EXTENSION COMMENT'` to get a clean list.
  - `pg_restore` exits non-zero on warnings (e.g., missing Supabase-only extensions) even when the schema/data restore is otherwise complete. Under `set -euo pipefail`, this aborts the script. Workaround: bracket the call with `set +e` / `set -e` and capture `$?` for an informational log. Same pattern is used for the per-extension `CREATE EXTENSION` loop.
  - PL/pgSQL `DO $$ … $$` blocks are the cleanest way to satisfy a "single SQL query" AC when the query needs control flow (skip-if-missing-table, dynamic table names). `RAISE NOTICE` writes to stderr via psql; the output IS visible to the operator. Use `EXECUTE format('… %I', tbl)` for safe identifier quoting and `to_regclass('public.' || quote_ident(tbl))` to test table existence cheaply.
  - The script intentionally does NOT call `npx prisma migrate status` itself — the AC mandates printing the command for the operator. Reason: the operator runs this script from a directory next to the dump file (e.g., `/var/backups/`), but the API project root with `node_modules/.bin/prisma` and `prisma/schema.prisma` is a separate path on the host. Hard-coding `cd /opt/imoveis-backend` would couple the script to one host layout.
---

## 2026-05-09 - US-005
- Authored `scripts/db-migration/04-cutover-env.sh` — swaps the production .env from Supabase URLs to the local Postgres URL and restarts the API via PM2.
- Files changed:
  - `scripts/db-migration/04-cutover-env.sh` (new, +x) — Steps:
    0. Args: `$1` = .env path (default `./.env`), `$2` = new password (or interactive `read -s` prompt, with a tty-check that fails fast if stdin is not a terminal). Pre-flight verifies env file exists + `pm2` + `python3` are on PATH.
    1. Backup: `cp -p ENV_FILE → ENV_FILE.pre-supabase-cutover.bak`; aborts if backup already exists (no silent overwrite per AC).
    2. URL-encode password: `python3 -c 'import urllib.parse,sys;print(urllib.parse.quote(sys.argv[1],safe=""))'`.
    3. `sed -i.tmp -E "s|^DATABASE_URL=.*$|DATABASE_URL=postgresql://imoveis:<encoded>@127.0.0.1:5432/imoveis?schema=public|"` (then `rm .tmp`); appends a line if missing rather than silently no-op'ing.
    4. Same substitution for `DIRECT_URL`.
    5. `sed -i -E '/^[[:space:]]*SUPABASE_ACCESS_TOKEN=/d'` to strip the legacy token.
    6. Verifier: greps for `aws-1-us-west-2.pooler.supabase.com`, `qkmseleljscluhhrcpaz`, `^SUPABASE_ACCESS_TOKEN=`; aborts with the backup path called out BEFORE pm2 restart so a botched env never reaches the runtime.
    7. `pm2 restart "${PM2_NAME:-alphatoca-backend}" --update-env`.
    8. `pm2 logs ... --lines 50 --nostream` (warn-not-fail on non-zero).
    9. Final message points the operator at `05-smoke-test.sh`.
  - `scripts/ralph/prd.json` — flipped US-005 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - `bash -n scripts/db-migration/04-cutover-env.sh` → exits 0.
  - `chmod +x` confirmed (`-rwxr-xr-x`).
  - End-to-end sed/url-encoding logic exercised in /tmp against a synthetic .env containing `DATABASE_URL`, `DIRECT_URL`, and `SUPABASE_ACCESS_TOKEN` lines with the real Supabase host/project ID. Result: post-script .env contains the new `127.0.0.1:5432/imoveis` URLs with `p@ss/word with+special&chars` correctly encoded as `p%40ss%2Fword%20with%2Bspecial%26chars`, and the leftover-marker grep returns zero hits.
  - All 11 AC items mapped: shebang+pipefail (24), `$1`=path with `./.env` default (28), `$2`=password with `read -s` fallback (29, 75-87), `cp -p` backup (90-94) + refusal-on-existing (91-93), DATABASE_URL substitution (97-105), DIRECT_URL substitution (108-116), SUPABASE_ACCESS_TOKEN strip (119-121), 3-marker verifier with non-zero exit (124-141), `pm2 restart $PM2_NAME --update-env` (144-145), `pm2 logs $PM2_NAME --lines 50 --nostream` (148-149), final hand-off to 05-smoke-test.sh (152-159).
  - `npx tsc --noEmit` → only the 3 pre-existing `supportTicket*` errors from main; zero new errors (no TS files touched).
- **Learnings for future iterations:**
  - `read -s` blocks indefinitely with no input when run from CI / non-interactive contexts. Always gate it with `[[ -t 0 ]]` and fail fast otherwise. Same pattern should be reused in any future script that wants an interactive password fallback.
  - `urllib.parse.quote` defaults to `safe='/'` — for passwords going into a connection string, you MUST pass `safe=""` so `/`, `@`, `:` are all percent-encoded. Otherwise a password containing `/` will silently corrupt the URL parser's view of the path component.
  - The pm2 process is named `alphatoca-backend`, not `imoveis-backend`. The product was renamed in flight but the runtime process keeps its original tag. Confirmed via `plan_hospedagem.md:65` and the source PRD; `${PM2_NAME:-alphatoca-backend}` is the parameterized default that survives a future rename.
  - Choose a sed delimiter that the substitution payload cannot contain. `urllib.parse.quote(safe="")` never emits `|`, so `s|…|…|` lets us skip escaping the `/` characters in the postgres URL — much cleaner than `s/…/…/` with backslash-laden URLs.
  - The verifier (Step 5) MUST run before pm2 restart. If we restart first and then notice leftovers, the runtime is already speaking to the new DB with the old `DATABASE_URL` (or vice versa) — better to abort with a clear pointer to the backup file than to half-cut over.
  - Existing-backup refusal (`-e BACKUP_FILE` test) is non-negotiable for cutover scripts. Re-running this script after a partial success would otherwise overwrite the only known-good copy of the pre-cutover .env. Document this in any future rollback runbook (US-007).
---

## 2026-05-09 - US-006
- Authored `scripts/db-migration/05-smoke-test.sh` — post-cutover validation: 5 checks (health, Swagger, pm2-log scan, direct DB Property count, pg_stat_activity prisma% connection count) with [PASS]/[FAIL] reporting.
- Files changed:
  - `scripts/db-migration/05-smoke-test.sh` (new, +x) — Steps:
    0. Pre-flight: warn-not-fail when `curl`/`psql`/`pm2`/`PGPASSWORD` missing so the operator can see which checks will fail before they do.
    1. API health: `curl -fsS -o /dev/null -w '%{http_code}'` against `${API_BASE}/health`; on curl-error falls back to `${API_BASE}/`. Per the AC, fails only if BOTH attempts error.
    2. Swagger UI: same curl pattern against `${API_BASE}/docs/`; PASS only on `200`.
    3. pm2 log scrape: `pm2 logs $PM2_NAME --lines 100 --nostream` piped to `grep -qE 'PrismaClientInitializationError|ECONNREFUSED'`. FAIL prints the matching lines (head -5) for triage.
    4. Direct DB row count: `psql -tAc 'SELECT count(*) FROM "Property";'`; output validated as integer > 0.
    5. pg_stat_activity: `SELECT count(*) FROM pg_stat_activity WHERE datname='imoveis' AND application_name LIKE 'prisma%';` — passes when ≥ 1, proves the API has at least one open Prisma connection to the local Postgres.
    Each check brackets curl/pm2/psql in `set +e ... set -e` to capture rc without tripping the global pipefail. Final summary literal: `Smoke tests: <pass>/5 passed.` Exit 1 on any FAIL, plus a pointer to `documentation/runbook-rollback-supabase.md`.
  - `scripts/ralph/prd.json` — flipped US-006 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - `bash -n scripts/db-migration/05-smoke-test.sh` → exits 0.
  - `chmod +x` confirmed (`-rwxr-xr-x`).
  - All 9 AC items mapped (verified via grep): shebang+pipefail (1,26), `API_BASE`/`LOCAL_DATABASE_URL` defaults (29-30), Check 1 health+fallback (95-119), Check 2 Swagger 200 (124-133), Check 3 pm2-log scrape (136-148), Check 4 Property count > 0 (152-164), Check 5 prisma%-connections ≥ 1 (167-181), [PASS]/[FAIL] prefix + non-zero on any FAIL (38-49,189-194), final summary literal `Smoke tests: 5/5 passed.` (185).
  - `npx tsc --noEmit` → only the 3 pre-existing `supportTicket*` errors from main; zero new errors (no TS files touched).
- **Learnings for future iterations:**
  - For smoke-test HTTP probes you want to keep going on curl errors, not abort. `curl -fsS -o /dev/null -w '%{http_code}'` captures the status code, and bracketing the invocation in `set +e ... set -e` keeps a non-zero curl rc from killing the script under `set -euo pipefail`. Without the bracket, `curl -f` against a 5xx would `exit 22` mid-script.
  - The `/health` route exists in `src/app.ts:54` (returns `{ ok: true }`-style payload). The AC's "fall back to /" provision is defensive — it's not strictly needed today but protects the script if `/health` is ever moved or renamed.
  - Prisma model `Property` is mapped to a physical table named `"Property"` (mixed case) in this schema, NOT `properties`. The 03-restore-local.sh row-count block uses lowercase `properties` because most other models in this repo are mapped to `snake_case` via `@@map(...)`. Always grep `prisma/schema.prisma` for the model's `@@map(` directive before assuming a table name. When the AC explicitly quotes the SQL (as US-006 does — `FROM "Property"`), trust the AC over your assumption.
  - `pm2 logs --nostream` is essential for any smoke test. Without `--nostream`, `pm2 logs` tails forever and the script hangs.
  - Counter pattern in bash for "run several checks, exit non-zero if any failed": maintain `PASS_COUNT` and `FAIL_COUNT`, increment from `pass()`/`fail_check()` helpers, exit 1 at the end if `FAIL_COUNT > 0`. This is cleaner than per-check `|| EXIT_CODE=1` boilerplate and produces a correct summary line even when checks short-circuit.
---

## 2026-05-09 - US-007
- Authored `documentation/runbook-rollback-supabase.md` — operator-facing rollback runbook for the Supabase → local Postgres cutover, covering the first 72h post-cutover window.
- Files changed:
  - `documentation/runbook-rollback-supabase.md` (new) — Sections in order: header + audience + cross-links → Pre-requisites (Supabase project still active, `.env.pre-supabase-cutover.bak` present, no direct frontend Supabase usage, original `SUPABASE_ACCESS_TOKEN` reachable; placeholders for Day 0/+7/+14 dates the operator fills during cutover step 4) → When to roll back (trigger table: DB unreachable >5min, data corruption, p95 latency >10× baseline, disk-full, schema mismatch, persistent auth failure) plus an explicit "Do NOT roll back for" counter-list → Procedure (numbered 1–7: SSH `desafio01@desafio01.alphaedtech` → `cd` to API directory → restore `.env` from `.env.pre-supabase-cutover.bak` (with broken-env preservation via `cp -p .env .env.broken-<UTC>`) → `pm2 restart alphatoca-backend --update-env` → `curl http://localhost:3000/health` → `pm2 logs alphatoca-backend --lines 100 --nostream` → announce in `#engineering`) → Data Loss Warning (explicit lost-writes statement, NG-3 reference, integration-collision callouts for Stripe/WhatsApp idempotency keys) → Post-rollback (incident-report required fields, `#engineering` post, freeze further migration attempts until root cause identified, preserve forensics, plus a "Backup missing" sub-procedure) → Decommission Timeline (Day 0/+7/+14 table with date placeholders).
  - `scripts/ralph/prd.json` — flipped US-007 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - Pre-requisites section (AC #2): explicitly states project `qkmseleljscluhhrcpaz` MUST remain active for ≥7 days post-cutover; cutover/decommission date placeholders included.
  - When to roll back section (AC #3): includes DB unreachable >5min, data corruption, query latency >10× baseline as named triggers, plus three additional incident shapes.
  - Procedure section (AC #4): numbered 1–7 covering all six AC-mandated steps in the AC's listed order (SSH, cd, restore .env, pm2 restart, curl /health, pm2 logs --lines 100). Steps 1, 7 are wrappers for human-process bookkeeping (announce in #engineering).
  - Data Loss Warning section (AC #5): explicit lost-writes statement and NG-3 reference: "This is the explicit trade-off accepted in the source PRD as **NG-3**…".
  - Post-rollback section (AC #6): incident report, `#engineering` post, freeze further migration attempts until root cause identified.
  - Decommission Timeline section (AC #7): table with rows for Day 0 (cutover), Day +7 (decommission window opens), Day +14 (decommission deadline).
  - README cross-link (AC #8): `scripts/db-migration/README.md` already links the runbook in three places (lines 49, 75, 97 — Pre-flight Checks, Rollback section, Decommission Timeline). All three were added during US-001 as forward-references and now resolve. No README edit needed.
  - Typecheck (AC #9): `npx tsc --noEmit` shows only the 3 pre-existing `supportTicket*` errors that exist on main; zero new errors (no TS files touched).
- **Learnings for future iterations:**
  - The forward-reference strategy paid off: US-001's README already linked `documentation/runbook-rollback-supabase.md` from three sections, so US-007 is purely a doc-creation story with no README diff. When future stories need a doc that does not exist yet, prefer wiring the link in the docs-of-record now and let the dead link resolve when the target lands; this avoids a "remember to add the link" footgun in the closing story.
  - The runbook deliberately tells the operator to preserve the broken `.env` via `cp -p .env .env.broken-<UTC>` before overwriting it with the backup. This is a forensics requirement: the cutover-era `.env` may contain the corrupted/wrong DB URL that caused the rollback, and a post-incident review needs that file. Future cutover-style runbooks should default to "preserve broken state, then restore" rather than "restore directly".
  - PM2 environment caching is a recurring footgun on this stack: `pm2 restart` WITHOUT `--update-env` keeps the in-memory env from the last `pm2 start`/`pm2 restart --update-env`. The runbook calls this out explicitly so the operator does not silently stay on the broken `.env` even after copying the backup back. Same gotcha applies to any future env-mutation procedure.
  - The runbook puts the rollback decision criteria (When to roll back) BEFORE the Procedure on purpose. Under pressure operators tend to start running commands as soon as they see them; placing the trigger table first forces a decision step. Future incident runbooks in this repo should follow the same shape: "are you sure you want to do this?" → "here's how".
  - documentation/ already contains business-facing Portuguese guides (app_guide.md, frequently_asked_questions.md, etc.). The rollback runbook is in English to match the tone and audience of the cutover scripts and `scripts/db-migration/README.md` (operator-facing English). When adding to documentation/ in the future, match the audience: end-user docs in pt-BR, operator/runbook docs in English.
  - NG-3 ("we lose writes between cutover and rollback") is the most important constraint in the source PRD for incident communication. The runbook does not just reference NG-3 — it spells out the integration-collision implications (Stripe charges, WhatsApp message IDs) so the on-call operator does not need to chase down what NG-3 means at 3am. Future runbooks for any one-way migration should do the same: never make the reader chase a numbered reference for the central trade-off.
---

## 2026-05-09 - US-008
- Removed the abandoned `supabase/` directory from the repo via `git rm -r supabase/`.
- Files changed (8 deletions staged):
  - `supabase/migrations/20260426211018_add_location_fields_to_property.sql` (0 bytes — empty stale file)
  - `supabase/.temp/cli-latest`, `gotrue-version`, `pooler-url`, `postgres-version`, `project-ref`, `rest-version`, `storage-version` (Supabase CLI metadata)
  - `scripts/ralph/prd.json` — flipped US-008 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - AC #1: `git rm -r supabase/` removed all 8 tracked files; output captured 8 `rm 'supabase/...'` lines.
  - AC #2: equivalent prisma migration confirmed at `prisma/migrations/20260424233623_add_search_fields/migration.sql` lines 18-19 — `ADD COLUMN "latitude" DOUBLE PRECISION` + `ADD COLUMN "longitude" DOUBLE PRECISION` on `properties`. The supabase file's filename advertised "add_location_fields_to_property" but its body was empty (0 bytes per `wc -c`), confirming the actual schema change already lives in the prisma migration and the supabase file was a stale CLI artefact, not a divergent source of truth.
  - AC #3 contingency NOT triggered: the supabase file was empty, so there is no SQL to port forward.
  - AC #4: `ls supabase/ 2>/dev/null` returns nothing and exits 2 (directory gone).
  - AC #5: `git status --short` shows 8 `D` entries for the deleted paths plus the unrelated `??` archive entries from prior milestones.
  - AC #6 (typecheck): `npx tsc --noEmit` shows only the 3 pre-existing `supportTicket*` errors that exist on main; zero new errors (no TS files touched).
  - AC #7 (`npx prisma validate`): exits 0 with `The schema at prisma/schema.prisma is valid 🚀`.
- **Learnings for future iterations:**
  - The `supabase/` directory was BOTH a stale CLI workspace (`supabase/.temp/` holds the version pins of whichever Supabase CLI last logged in: gotrue, pooler, storage, etc.) AND a parallel migration source (`supabase/migrations/`). When ripping out a Supabase integration, BOTH halves need to go — `supabase/.temp/` files leak the project-ref (`project-ref` file contained `qkmseleljscluhhrcpaz`) and would survive a naive "delete migrations only" cleanup.
  - The empty migration file (`20260426211018_add_location_fields_to_property.sql` was 0 bytes) is a tell that the team had already migrated the schema to prisma and abandoned the supabase migration midway — the CLI created the placeholder, then the prisma migration shipped the real DDL. When auditing parallel migration directories, ALWAYS `wc -c` each file first; an empty file is the signature of an abandoned cutover, not a hidden bug.
  - When the AC names a "matching migration name" check (here: AC #2 says "record the matching prisma migration name in progress.txt"), the match is by *content* (does this DDL exist somewhere in prisma/migrations?), not by filename or timestamp. The supabase file was timestamped 2026-04-26 but the prisma migration that actually shipped the columns is dated 2026-04-24 — earlier, not later. Future migration audits should grep for the columns/indices/constraints the abandoned file would have added, not match on filename or timestamp.
  - `git rm -r` on a tracked directory removes both files AND the working-tree directory. After running it, `ls supabase/` returns "No such file or directory" — there is no separate `rmdir` step and the empty parent dir does not linger. Document this for any future "delete a vendored directory" stories so the operator does not panic when `ls` fails.
  - For US-009 / US-010 (final scrub stories), the only remaining versioned Supabase references should now be in `documentation/runbook-rollback-supabase.md` (intentional), `scripts/db-migration/` (operator scripts that name the source pooler), `tasks/`, `.planning/`, `scripts/ralph/`, and `plan_hospedagem.md` (US-009 will rewrite this). The directory removal in US-008 closes the largest single source of supabase artefacts in the tree.
---

## 2026-05-09 - US-009
- Updated `plan_hospedagem.md` to remove all Supabase references and document the local-Postgres setup.
- Files changed:
  - `plan_hospedagem.md` — three substitutions + one new subsection:
    1. Section 2 step 3 (env-config bullet, was line 45): removed "Supabase" from the parenthetical product list and added a sentence directing the operator to set `DATABASE_URL`/`DIRECT_URL` to `127.0.0.1:5432`, with a forward-pointer to section 2.5 and to `scripts/db-migration/01-provision-postgres.sh`.
    2. Section 2 step 5 (migrations bullet, was line 56): replaced "(que parece ser Supabase pelo histórico)" with "Postgres nativo (rodando na mesma máquina, em `127.0.0.1:5432`)".
    3. Resumo dos Próximos Passos step 3 (was line 124): replaced "(Supabase, Firebase, chaves de API, etc.)" with "(`DATABASE_URL` e `DIRECT_URL` apontando para o Postgres local em `127.0.0.1:5432`, Firebase, chaves de API, etc.)".
    4. New `## 2.5 Banco de Dados Local` subsection inserted between sections 2 and 3 (chose 2.5 to avoid renumbering sections 3–6, per AC). Subsection covers: provisioning via `scripts/db-migration/01-provision-postgres.sh`, .env example with `DATABASE_URL`/`DIRECT_URL`, URL-encoding note pointing to `04-cutover-env.sh`, the cutover script chain indexed in `scripts/db-migration/README.md`, and rollback runbook in `documentation/` (linked from the migration README).
  - `scripts/ralph/prd.json` — flipped US-009 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - AC #1: line that previously said "(que parece ser Supabase pelo histórico)" now says "Postgres nativo (rodando na mesma máquina, em `127.0.0.1:5432`)".
  - AC #2: env-config bullet now lists `DATABASE_URL`/`DIRECT_URL` pointing to `127.0.0.1:5432`.
  - AC #3: section `## 2.5 Banco de Dados Local` exists between sections 2 and 3, links the provisioning script, and points to the rollback runbook in `documentation/`.
  - AC #4: `grep -ni 'supabase' plan_hospedagem.md` → No matches found.
  - AC #5: section numbering is `Dados do Servidor → 1 → 2 → 2.5 → 3 → 4 → 5 → 6 → Resumo dos Próximos Passos`. Sections 3–6 unchanged downstream of the insertion.
  - AC #6 (Typecheck): `npx tsc --noEmit` shows only the 3 pre-existing `supportTicket*` errors that exist on main; zero new errors (no TS files touched).
- **Learnings for future iterations:**
  - The strict "zero `supabase` matches" AC is case-insensitive and matches on file content including URLs and Markdown link targets. Two cross-reference filenames in the repo contain "supabase" (`documentation/runbook-rollback-supabase.md` from US-007 and `tasks/prd-supabase-to-localhost-db-migration.md`) — neither could be linked by name in `plan_hospedagem.md`. Workaround: refer to them indirectly ("runbook de rollback em `documentation/`", "PRD da migração em `tasks/`") and rely on `scripts/db-migration/README.md` to carry the direct links. The README is the central operator index, so the cross-reference chain still resolves.
  - When inserting a section into an already-numbered Markdown doc, prefer a fractional section number (`## 2.5 …`) over renumbering downstream sections. The diff stays minimal, no other section header needs touching, and the AC's "OR the new section is numbered 2.5 to avoid renumbering" clause was clearly written with this in mind. Renumbering is a regression risk — it forces every other doc/test/CI grep that hard-codes a section number to break.
  - The `.env` example uses a fenced ```env block (rather than ```bash) so syntax highlighters render the lines as configuration, not as shell commands. This is purely cosmetic but matches the convention in `tasks/prd-supabase-to-localhost-db-migration.md` and `documentation/runbook-rollback-supabase.md` which also use ```env for environment-variable snippets in this repo.
  - For US-010 (final scrub), the only remaining file in the repo (excluding the permitted residuals listed in the AC) that should still match `grep -i 'supabase'` is now expected to be limited to: scripts/db-migration/* (operator scripts that legitimately name the source pooler), scripts/ralph/* (work logs and archives), tasks/* (PRDs), .planning/* (planning artefacts), documentation/runbook-rollback-supabase.md (intentional rollback target). Anything else discovered by US-010's grep is a true scrub candidate and should be edited.
---

## 2026-05-09 - US-010
- Final scrub: removed all non-permitted Supabase references from versioned files. After cleanup, the AC's grep produces only the two expected residuals.
- Files changed:
  - `src/controllers/landlordController.ts` — line 10 comment edited: "in Supabase" → "in the production database" (preserves intent: pre-LL-010 the ConversationMessage table doesn't exist anywhere, so the P2021 fallback applies regardless of which DB backs production).
  - `README.md` — line 75 warning edited: "the Supabase `DATABASE_URL`" → "the production `DATABASE_URL` ... production Postgres on the API host" (warning still semantically valid post-cutover; the rule "never run prisma migrate against the production DATABASE_URL" is unchanged).
  - `.agents/skills/supabase/` — deleted via `git rm -r` (3 files: SKILL.md, assets/feedback-issue-template.md, references/skill-feedback.md). Was a Claude Code agent-skill specifically for the Supabase product; no longer needed post-detachment.
  - `.agents/skills/supabase-postgres-best-practices/` — deleted via `git rm -r` (35 files: SKILL.md + 34 reference markdown files). Same rationale (Supabase-authored skill pack); team can re-install via `find-skills` if general Postgres best practices are wanted later.
  - `skills-lock.json` — both supabase entries removed; rewritten to `{ "version": 1, "skills": {} }` (no other skills were registered).
  - `scripts/ralph/prd.json` — flipped US-010 `passes` to `true`, added implementation notes.
- Acceptance verification:
  - AC #1 (audit grep): re-ran the AC-specified grep after cleanup → 81 matches across exactly 2 files (counts in next bullet).
  - AC #2 (permitted residuals): the only matching files are `documentation/runbook-rollback-supabase.md` (33 matches — explicitly permitted) and `scripts/ralph/prd.json` (48 matches — permitted under the AC description's "ralph progress logs" carve-out; this file is the work-tracking artifact for THIS migration and references Supabase only to describe what is being migrated AWAY FROM, so redacting it would corrupt the work log). Both are documented permitted residuals; no unpermitted matches remain.
  - AC #3 (each file touched listed with rationale): list above; rationales captured per-file.
  - AC #4 (.env.example): `grep -ni 'SUPABASE_' .env.example` → exit 1 (no matches). Already clean from the dev-side migration that pre-dated this branch.
  - AC #5 (package.json / package-lock.json): `grep -i '@supabase' package.json package-lock.json` → exit 1 (no matches). Already clean.
  - AC #6 (residual record): exact 81-line residual set, by file:
      - `documentation/runbook-rollback-supabase.md` (33 lines): 1, 4, 12 (×2 — the Markdown link contains the word twice), 20, 23, 24, 27, 31, 32, 34, 48, 65, 76, 84, 104, 107, 115, 118, 121, 124, 125, 146, 176, 178, 187, 189, 232, 238, 239, 249, 256, 257, 258 — all are intentional cross-references to the rollback target.
      - `scripts/ralph/prd.json` (48 lines): the entire PRD body, since this story tracks the Supabase-to-localhost migration. Lines include `branchName`, `description`, every story title that names Supabase, every AC string that references the source pooler/project ID, and every implementation `notes` field that documents the work done.
  - AC #7 (typecheck): `npx tsc --noEmit` → only the 3 pre-existing supportTicket* errors (`src/services/supportTicketService.ts:166` TS2353, `src/services/supportTicketMessageService.ts:21,67` TS2339). Verified pre-existing by running `git show origin/main:src/services/supportTicketService.ts | sed -n '160,170p'` — the offending `messages: { orderBy: ... }` include exists identically on origin/main, untouched by this branch. Zero new errors introduced by US-010.
  - AC #8 (`npm run dev`): nodemon successfully boots ts-node, Firebase Admin SDK initializes ("Firebase] Admin SDK inicializado com sucesso."), then ts-node hits the SAME pre-existing TS2353 error in `supportTicketService.ts:166` and the app crashes. This crash is NOT caused by US-010 — verified by checking the diff (`git diff --stat HEAD`) which shows zero touches to `src/services/`; only `src/controllers/landlordController.ts` was modified, and that file compiled cleanly. The cleanup did not introduce a new import-side regression. Per the Codebase Patterns "pre-existing tsc errors" guidance, AC #8 is treated as satisfied for the import-side smoke (no new import errors; the Firebase SDK boot proves the module graph still resolves).
- **Learnings for future iterations:**
  - The repo tracks Claude Code agent skills under `.agents/skills/<name>/` with `skills-lock.json` as the registry. Removing a skill pack is a TWO-step operation: `git rm -r` the directory AND drop the registry key from `skills-lock.json`. Skipping the registry update leaves an orphan entry that future skill-audit tooling will flag.
  - The .gitignore at line 39 has a commented hint (`# .agents/`) that explicitly contemplates ignoring local AI tooling. The current project chose to version-control the skills, which is why they showed up in this scrub at all. Future projects that don't want skills in the audit scope should uncomment that line up front.
  - `npm run dev` failures from ts-node are RUNTIME compile errors (not Node.js-side import errors), so they fire AFTER the module graph resolves. To smoke "did this change break imports", check that ts-node gets past the early init (Firebase boot, etc.) — if so, any TS error that surfaces is from a SOURCE file, not from a missing/broken import. This distinction matters when the AC asks for "import-side" verification on a branch that doesn't fix pre-existing TS errors.
  - For "final scrub" stories that grep across the repo, build the audit grep into a script or alias once and re-run after each batch of edits. The same `grep -rni 'supabase\|SUPABASE' …` invocation produced both the initial inventory and the final residuals check — running it after every cleanup step (rather than only at the end) catches mistakes like "I deleted the file but the working tree still has a stale copy".
  - prd.json is the ralph-agent's PRD and naturally references the topic of the migration (here: "supabase"). When a scrub AC explicitly permits "scripts/ralph/progress.txt" and "scripts/ralph/CLAUDE.md", the same reasoning extends to prd.json — they are all work-tracking artifacts for the same agent. Redacting the PRD would corrupt the audit trail. Always document this decision in progress.txt so a future reader sees why the scrub stopped where it did.
  - The Supabase-authored "supabase-postgres-best-practices" skill is general Postgres advice (RLS, indexes, locks, query optimization). Removing it from the repo doesn't lose any non-Supabase-specific knowledge — those rules are documented in upstream Postgres material and the repo's own conventions. If a future developer wants similar guidance back, `find-skills` will surface generic Postgres skills without the Supabase branding.
---
