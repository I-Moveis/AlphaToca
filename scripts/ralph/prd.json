{
  "project": "I-Moveis",
  "branchName": "ralph/fix-image-serving-mismatch",
  "description": "Em produção (desafio01.alphaedtech / 10.10.0.201) o app Flutter não renderiza as imagens dos imóveis. Causa raiz: src/services/propertyImageStorageService.ts:56 grava URLs como `/uploads/<id>/<file>.jpg` (sem o segmento `/api`), mas o `express.static` em src/app.ts:51 está montado em `/api/uploads`. O Flutter monta `baseUrl + image.url` e cai em 404. Fix: 100% backend, manter URLs no banco como estão, adicionar um segundo `express.static` em `/uploads` (alinhado com o que o Flutter já espera) mantendo `/api/uploads` como compat. Sem migration de dados, sem deploy do cliente Flutter. Source PRD: tasks/prd-fix-image-serving-mismatch.md.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Adicionar mount estatico em /uploads mantendo /api/uploads (compat) com CORP e cache headers",
      "description": "As an API operator, I want the static file handler to also respond at `/uploads` so that stored relative URLs (`/uploads/<propertyId>/<file>.jpg`) resolve when the Flutter client concatenates them onto a baseUrl without the `/api` segment, with no client change.",
      "acceptanceCriteria": [
        "Em `src/app.ts`, adicionar `app.use('/uploads', express.static(path.join(__dirname, '../uploads'), { setHeaders }))` imediatamente ao lado do mount existente em `/api/uploads`",
        "O mount existente `app.use('/api/uploads', express.static(...))` permanece (atualizado para usar o mesmo `setHeaders`)",
        "Ambos os mounts ficam ANTES de `app.use('/api', authStack, ...)` para nao passarem por `checkJwt`",
        "O `setHeaders` injeta `Cross-Origin-Resource-Policy: cross-origin` e `Cache-Control: public, max-age=86400` nas respostas dos dois mounts (extrair para uma constante/funcao compartilhada para evitar duplicacao)",
        "Comentario inline em `src/app.ts` explica por que existem dois mounts: URLs historicas em `PropertyImage.url` e `Contract.pdfUrl` foram gravadas como `/uploads/...`; o mount `/uploads` resolve isso sem migracao e o `/api/uploads` e mantido por retrocompatibilidade",
        "`npm run build` passa sem erros de tipo",
        "`grep -n \"express.static\" src/app.ts` mostra dois resultados (um em `/uploads` e outro em `/api/uploads`)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Implemented 2026-05-13. src/app.ts now mounts express.static at both /uploads (new) and /api/uploads (compat), sharing a setUploadsHeaders function that injects Cross-Origin-Resource-Policy: cross-origin and Cache-Control: public, max-age=86400. Inline comment explains the dual mount and references tasks/prd-fix-image-serving-mismatch.md. Both mounts sit before the authStack and the /api routes (lines 66-67), so they do not pass through checkJwt. `npm run build` passes (0 errors). `grep -n express.static src/app.ts` returns exactly two lines (66 and 67). Note: PRD text referenced an existing mount at /api/uploads, but the actual code on main HEAD had /uploads only (reverted by an earlier hotfix). End state matches PRD intent regardless."
    },
    {
      "id": "US-002",
      "title": "Teste de integracao Vitest cobrindo os dois mounts e bloqueio de path traversal",
      "description": "As a developer, I want a Vitest integration test that asserts both `/uploads` and `/api/uploads` serve a real image file from the `uploads/` directory AND reject path-traversal attempts, so that a future refactor that removes one mount fails CI instead of production.",
      "acceptanceCriteria": [
        "Novo arquivo `tests/uploadsStaticMount.test.ts` usando o mesmo padrao `supertest` + `request(app)` de `tests/contractPdf.test.ts`",
        "Teste cria um arquivo fixture (ex.: `uploads/test-fixture/sample.png` com 1x1 PNG bytes) no `beforeAll` e remove no `afterAll` (incluindo o diretorio)",
        "`GET /uploads/test-fixture/sample.png` retorna 200 com `content-type` iniciando em `image/`",
        "`GET /api/uploads/test-fixture/sample.png` retorna 200 com `content-type` iniciando em `image/`",
        "Resposta do GET 200 inclui header `cross-origin-resource-policy: cross-origin`",
        "Resposta do GET 200 inclui header `cache-control` contendo `max-age`",
        "`GET /uploads/../package.json` NAO retorna 200 (espera 403 ou 404 - `express.static` normaliza e bloqueia)",
        "`GET /api/uploads/../package.json` NAO retorna 200",
        "`GET /uploads/%2e%2e/package.json` NAO retorna 200",
        "`npm test -- tests/uploadsStaticMount.test.ts` passa",
        "`npm run build` passa sem erros de tipo"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Implemented 2026-05-13. New file tests/uploadsStaticMount.test.ts (9 tests, all passing) covers the dual mount and path-traversal hardening. Fixture (1x1 PNG bytes) written to uploads/test-fixture/sample.png in beforeAll and removed in afterAll. Asserts: (a) GET /uploads/test-fixture/sample.png and (b) GET /api/uploads/test-fixture/sample.png both return 200 with content-type matching /^image//; (c) cross-origin-resource-policy: cross-origin header is present on both; (d) cache-control header carries max-age on both; (e) /uploads/../package.json, /api/uploads/../package.json, and /uploads/%2e%2e/package.json all return non-200. `npm test -- tests/uploadsStaticMount.test.ts` => 9/9 pass. `npm run build` => exit 0. Also fixed a pre-existing malformed JSON in scripts/ralph/prd.json (US-002 was missing its object-open boundary, so jq could not parse the file)."
    },
    {
      "id": "US-003",
      "title": "Documentar o duplo mount em BACKEND_HANDOFF.md",
      "description": "As a future maintainer, I want the dual mount and its motivation documented outside the source file so that the `/api/uploads` vs `/uploads` choice is not undone by accident in a refactor and the rationale is discoverable from the handoff doc.",
      "acceptanceCriteria": [
        "Nova secao em `BACKEND_HANDOFF.md` chamada \"Servimento de imagens (uploads/) - duplo mount\" (ou secao equivalente expandida)",
        "Secao explica: (a) onde as URLs sao geradas (`propertyImageStorageService.savePropertyImages`, `contractDocumentStorageService.saveContractDocument`), (b) o formato armazenado (`/uploads/<id>/<file>.ext`), (c) por que existem DOIS mounts em `app.ts`, (d) que NAO ha migration de dados",
        "Secao referencia o PRD `tasks/prd-fix-image-serving-mismatch.md` como fonte autoritativa",
        "Secao menciona que PDFs de contrato continuam sendo servidos pelo endpoint protegido `GET /api/contracts/:id/pdf` - o duplo mount nao substitui esse fluxo",
        "Secao alerta o time de infra de que `uploads/` faz parte do backup do servidor (nao e regeneravel)",
        "`grep -i 'uploads' BACKEND_HANDOFF.md` encontra a nova secao"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Implemented 2026-05-13. Added a new top-level section in BACKEND_HANDOFF.md titled 'Servimento de imagens (uploads/) — duplo mount' placed immediately before 'Documentos de referência'. Section covers: (a) URL generation sites — propertyImageStorageService.savePropertyImages (PropertyImage.url) and contractDocumentStorageService.saveContractDocument (Contract.pdfUrl); (b) stored format /uploads/<propertyId>/<file>.ext and /uploads/contract-<contractId>/<file>.pdf; (c) the dual mount at src/app.ts lines 66-67, why both prefixes exist, and the shared setUploadsHeaders callback (CORP + Cache-Control); (d) explicit statement that there is NO data migration. References tasks/prd-fix-image-serving-mismatch.md as the authoritative source. Notes that contract PDF downloads continue through the protected GET /api/contracts/:id/pdf endpoint and are NOT replaced by the dual mount. Alerts infra that uploads/ must be in the server backup because the files are not regeneratable. Mentions tests/uploadsStaticMount.test.ts as the regression guard. `grep -i 'uploads' BACKEND_HANDOFF.md` matches 13 lines, all from the new section. `npm run build` => exit 0."
    }
  ]
}
